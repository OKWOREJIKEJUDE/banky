import 'package:flutter/material.dart';

class Annuities extends StatelessWidget {
  const Annuities({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}